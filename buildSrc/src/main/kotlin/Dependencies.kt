object AndroidX {
    private const val core_ktx_version = "1.6.0-alpha03"
    private const val appcompat_version = "1.3.1"
    private const val test_runner_version = "1.4.0"
    private const val espresso_core_version = "3.4.0"

    const val core_ktx = "androidx.core:core-ktx:$core_ktx_version"
    const val appcompat = "androidx.appcompat:appcompat:$appcompat_version"
    const val test_runner = "androidx.test:runner:$test_runner_version"
    const val espresso_core = "androidx.test.espresso:espresso-core:$espresso_core_version"
}

object Google {
    private const val material_version = "1.4.0-beta01"

    const val material = "com.google.android.material:material:$material_version"
}

object Constraint {
    private const val version = "1.1.2"

    const val constraint = "com.android.support.constraint:constraint-layout:$version"
}

object Moxy {
    private const val version = "2.2.2"

    const val moxy = "com.github.moxy-community:moxy:$version"
    const val android_x = "com.github.moxy-community:moxy-androidx:$version"
    const val ktx = "com.github.moxy-community:moxy-ktx:$version"
    const val compiler = "com.github.moxy-community:moxy-compiler:$version"
}

object Toothpick {
    private const val version = "3.1.0"

    const val runtime = "com.github.stephanenicolas.toothpick:toothpick-runtime:$version"
    const val ktp = "com.github.stephanenicolas.toothpick:ktp:$version"
    const val compiler = "com.github.stephanenicolas.toothpick:toothpick-compiler:$version"
}

object Epoxy {
    private const val version = "4.4.4"

    const val epoxy = "com.airbnb.android:epoxy:$version"
    const val processor = "com.airbnb.android:epoxy-processor:$version"
}

object Cicerone {
    private const val version = "5.0.0"

    const val cicerone = "ru.terrakok.cicerone:cicerone:$version"
}

object Rx {
    private const val rx_java_version = "3.1.1"
    private const val rx_android_version = "3.0.0"
    private const val rx_relay_version = "3.0.1"

    const val rx_java = "io.reactivex.rxjava3:rxjava:$rx_java_version"
    const val rx_android = "io.reactivex.rxjava3:rxandroid:$rx_android_version"
}

object Retrofit {
    private const val version = "2.9.0"

    const val retrofit = "com.squareup.retrofit2:retrofit:$version"
    const val gson = "com.squareup.retrofit2:converter-gson:$version"
    const val adapter = "com.squareup.retrofit2:adapter-rxjava3:$version"
}

object OkHttp {
    private const val version = "3.12.0"

    const val okhttp_server = "com.squareup.okhttp3:mockwebserver:$version"
    const val okhttp_logging = "com.squareup.okhttp3:logging-interceptor:$version"
}

object Lottie {
    private const val lottie_version = "5.0.3"

    const val lottie_animation = "com.airbnb.android:lottie:$lottie_version"
}

object Picasso {
    private const val version = "2.71828"
    const val picasso = "com.squareup.picasso:picasso:$version"
}

object JodaTime {
    private const val version = "2.10.9"

    const val joda_time = "joda-time:joda-time:$version"
}